# Clipt 1.0.0

첫 정식 버전. 0.1.0의 기능에 녹화 중 표시의 작은 수정 두 가지를 더했고, 정식 배포 버전으로 번호를 올렸다.

## 수정

- 영역·요소 녹화에서 녹화 위젯을 둘 빈 모서리가 없으면 위젯을 숨겨 영상에 찍히지 않게 함(팝업·단축키로 제어)
- 요소 따라가기 녹화가 요소가 잠시 숨겨진 상태에서 시작돼도 요소가 보일 때 정상 크기로 녹화

## 기능

- **스크린샷**: 보이는 화면, 전체 페이지(자동 스크롤 이어 붙이기), 요소, 영역
- **녹화**: 탭, 영역, 요소. 요소 녹화는 스크롤·이동에도 요소를 따라가는 "요소 따라가기" 지원
- **요소 선택**: 호버 → 클릭 고정 → 선택 패널에서 경로·깊이 조정, 키보드 조작
- **녹화 옵션**: MP4 / WebM(VP9·VP8·AV1), 24·30·60fps, 해상도 배율, 탭 소리·마이크, 카운트다운, 일시정지, 최대 길이, 녹화 중 표시(없음·테두리·위젯)
- **결과 페이지**: 미리보기, 다운로드, 클립보드 복사, 중단된 녹화 복구
- **단축키**: Alt+Shift+1~4(보이는 화면·영역·요소 캡처, 탭 녹화 시작/중지), `chrome://extensions/shortcuts`에서 변경
- **언어**: 한국어, 영어

## 권한과 개인정보

`activeTab`으로 사용자가 실행한 탭에만 접근하며 host 권한을 요구하지 않는다. 캡처·녹화 결과는 브라우저 안에만 저장하고 어떤 서버로도 보내지 않는다. 설정은 Chrome 동기화 저장소에 저장되어 Chrome 동기화를 켠 경우 사용자의 다른 기기와 동기화될 수 있다. 자세한 내용은 [PRIVACY.md](https://github.com/dydtjr1128/clipt/blob/v1.0.0/PRIVACY.md).

## 검증

정적 검사, 단위 테스트 154개, 확장을 실제로 로드한 E2E 87개(Windows·Linux CI)를 통과했다. 10분 이상 녹화 시 메모리, 1080p 30fps 유지, 탭 소리·마이크 믹싱은 실제 Chrome에서의 수동 확인이 남아 있다([#11](https://github.com/dydtjr1128/clipt/issues/11), [#12](https://github.com/dydtjr1128/clipt/issues/12)).

## 설치와 업데이트

[설치 안내](https://github.com/dydtjr1128/clipt/blob/v1.0.0/INSTALL.md) · [ZIP 다운로드](https://github.com/dydtjr1128/clipt/releases/download/v1.0.0/clipt.zip)

0.1.0을 압축 해제로 설치했다면 같은 폴더의 파일을 새 ZIP 내용으로 교체하고 `chrome://extensions`에서 새로고침한다. 설정과 저장된 결과는 유지된다. ZIP 체크섬은 **SHA256SUMS.txt**에 있다.
